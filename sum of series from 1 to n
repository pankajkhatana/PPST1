#include <iostream>
using namespace std;

int main() {
    int n, i, sum = 0;

    cout << "Enter the value of n: ";
    cin >> n;

    // Logic: Add numbers from 1 to n
    for(i = 1; i <= n; i++) {
        sum = sum + i;
    }

    cout << "The Sum of the series is: " << sum;

    return 0;
}
